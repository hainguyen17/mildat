import React from 'react';

const CheckButton = () => (
  <img
    className="appLogo"
    src={`${process.env.PUBLIC_URL}/checkbutton.svg`}
    alt="check-button"
  />
);

export default CheckButton;
